# Frontend Environment Variables for Local Development
# Copy this file to .env.local and modify as needed

# Backend API URL - for local development
NEXT_PUBLIC_BACKEND_URI=http://localhost:9000/api

# Socket service URLs - for local development
NEXT_PUBLIC_SOCKET_MIDDLEWARE_URI=http://localhost:8000

# Deployment environment (leave empty for auto-detection or set to 'local', 'docker', 'kubernetes')
# DEPLOYMENT_ENV=local
