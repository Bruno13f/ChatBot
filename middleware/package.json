{
  "name": "middleware",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "bun run index.ts",
    "dev": "nodemon index.ts"
  },
  "dependencies": {
    "nodemon": "^3.1.10"
  }
}
