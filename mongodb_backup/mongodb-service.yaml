apiVersion: v1
kind: Service
metadata:
  name: mongodb-backup
  labels:
    app: mongodb-backup
spec:
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
  selector:
    app: mongodb-backup
  type: ClusterIP
