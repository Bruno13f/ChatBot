apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose --file docker-compose.yml convert
    kompose.version: 1.36.0 (HEAD)
    keel.sh/policy: force          
    keel.sh/trigger: poll           
    keel.sh/pollSchedule: "@every 1m"
  labels:
    io.kompose.service: sockets-open-ai
  name: sockets-open-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      io.kompose.service: sockets-open-ai
  template:
    metadata:
      annotations:
        kompose.cmd: kompose --file docker-compose.yml convert
        kompose.version: 1.36.0 (HEAD)
      labels:
        io.kompose.service: sockets-open-ai
    spec:
      containers:
        - name: sockets-open-ai
          image: docker.io/projetocn/projeto-cn-sockets-open-ai:production
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: env-configmap
          ports:
            - containerPort: 4002
      restartPolicy: Always
